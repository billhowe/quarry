#!/usr/bin/env python

import string
def AddItem(name, val, desc, type):
  f = file('/tmp/pythonoutput.tmp', 'a')
  f.write(';'.join([name, val, desc, type, '\n']))
  f.flush()

# AddItem('foo', 'bar', 'foomer', 'string') 
